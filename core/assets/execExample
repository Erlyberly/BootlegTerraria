//A line that starts with two slashes(/) is a comment and wont be executed
//Any command without it will be executed as if it was typed in the console
bind jump space //comments can also be at the end of a line
wpn sword
gravity 4